# The .env file should be populated with your OPENAI_API_KEY, which is data that should be kept private.
# That is why this file should be ignored by git, and should not be uploaded to the GitHub repo,
# but the file should still exist in order for the project to run.
# You have to recreate it manually, using your own key.
static/.env